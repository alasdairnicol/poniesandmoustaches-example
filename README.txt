Instructions
============

Clone the repository

Create a virtual environment. Install the requirements.

    pip install -r requirements.txt

Run the dev server

    ./manage.py runserver

You can then view the index page at http://127.0.0.1:8000
